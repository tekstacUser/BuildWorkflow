name: Sample Workflow

on:
  push:
    branches:
      - main

jobs:
  print-message:
    runs-on: ubuntu-latest
    steps:
      - name: print-welcome-message
        run: echo "Hello, GitHub Actions!"
